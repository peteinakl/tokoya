---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionHead from '../components/SectionHead.astro';
import BookingButton from '../components/BookingButton.astro';
import { getCollection } from 'astro:content';

const locations = await getCollection('locations');
const ponsonby = locations.find((l) => l.data.slug === 'ponsonby')!;
const remuera = locations.find((l) => l.data.slug === 'remuera')!;

const shopImages = [
  { src: '/images/shops/ponsonby-interior.png', title: 'Ponsonby Barber Stations', desc: 'Custom timber stations, warm vintage lighting, and classic barber chairs on Ponsonby Road.' },
  { src: '/images/shops/remuera-salon.png', title: 'Remuera Barber & Nail Salon', desc: 'Our dual-craft salon at 352 Remuera Road, designed for haircuts, beard grooming, and specialized nail treatments.' },
  { src: '/images/shops/ponsonby-chairs.png', title: 'The Ponsonby Chair Lineup', desc: 'Punctual, spotless chair arrangement where clients experience unhurried Japanese hospitality.' },
  { src: '/images/hero/hero-chair.png', title: 'Traditional Reclining Barber Chair', desc: 'Heavyweight ergonomic chairs engineered for precise hot towel shaves and beard sculpting.' }
];

const craftImages = [
  { src: '/images/craft/scissors-clippers.png', title: 'Precision Clipper & Shear Work', desc: 'Sterilized shears and high-torque detailing clippers prepared before each appointment.' },
  { src: '/images/craft/barber-chair-tools.png', title: 'The Barbering Station', desc: 'Straight razors, hot lather warmers, and imported styling balms kept at hand.' },
  { src: '/images/hero/hero-craft.png', title: 'The Art of the Scissor Cut', desc: 'Attentive, millimeter-accurate hand scissor work tailored to each client’s facial geometry.' },
  { src: '/images/hero/hero-station.png', title: 'Apothecary & Product Shelf', desc: 'Stocked with official Layrite, Uppercut Deluxe, and Proraso wet shaving formulations.' }
];

const teamPhotos = [
  { src: '/images/barbers/kris-torcato.jpg', name: 'Kris Torcato', title: 'Senior Barber', loc: 'Ponsonby' },
  { src: '/images/barbers/jojo-parido.jpg', name: 'Jojo Parido', title: 'Senior Barber', loc: 'Remuera' },
  { src: '/images/barbers/kim-duong.jpg', name: 'Kim Duong', title: 'Senior Barber', loc: 'Remuera' },
  { src: '/images/barbers/justin-park.jpg', name: 'Justin Park', title: 'Barber & Stylist', loc: 'Ponsonby & Remuera' },
  { src: '/images/barbers/james.jpg', name: 'James', title: 'Master Craftsman', loc: 'Ponsonby' },
  { src: '/images/barbers/etsuya-kidoguchi.jpg', name: 'Etsuya Kidoguchi', title: 'Master Barber', loc: 'Ponsonby' }
];

const title = "Gallery & Craft Portfolio | Tokoya Barber & Shop Auckland";
const description = "Explore the photography and craft of Tokoya Barber & Shop. View our Ponsonby and Remuera interiors, precision barbering tools, and master craftsmen.";
---

<BaseLayout title={title} description={description}>

  <!-- Hero -->
  <section class="gallery-hero">
    <div class="gallery-hero__bg" style="background-image: url('/images/hero/hero-craft.png');"></div>
    <div class="gallery-hero__overlay"></div>
    <div class="wrap gallery-hero__content">
      <span class="label">Atmosphere &middot; Detail &middot; Discipline</span>
      <h1 class="display gallery-hero__title">The Craft In Focus</h1>
      <p class="lede gallery-hero__lede">
        A visual journey through our Ponsonby barbershop and Remuera salon.
        Immaculate hygiene, authentic Japanese discipline, and honest craftsmanship.
      </p>
    </div>
  </section>

  <!-- Section 1: The Shops -->
  <section class="section on-deep">
    <div class="wrap">
      <SectionHead label="The Spaces" title="Two Distinct Auckland Locations" />
      <div class="gallery-grid">
        {shopImages.map((img) => (
          <figure class="gallery-card">
            <div class="gallery-card__img-wrap">
              <img src={img.src} alt={img.title} loading="lazy" />
            </div>
            <figcaption class="gallery-card__caption">
              <h3 class="gallery-card__title">{img.title}</h3>
              <p class="gallery-card__desc">{img.desc}</p>
            </figcaption>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <!-- Section 2: Craft & Discipline -->
  <section class="section on-ink">
    <div class="wrap">
      <SectionHead label="The Discipline" title="Precision Tools &amp; Rituals" />
      <div class="gallery-grid">
        {craftImages.map((img) => (
          <figure class="gallery-card">
            <div class="gallery-card__img-wrap">
              <img src={img.src} alt={img.title} loading="lazy" />
            </div>
            <figcaption class="gallery-card__caption">
              <h3 class="gallery-card__title">{img.title}</h3>
              <p class="gallery-card__desc">{img.desc}</p>
            </figcaption>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <!-- Section 3: The Craftsmen -->
  <section class="section on-deep">
    <div class="wrap">
      <SectionHead label="Behind The Shears" title="The Tokoya Barbers" />
      <div class="barbers-photo-grid">
        {teamPhotos.map((barber) => (
          <div class="barber-portrait-card">
            <div class="portrait-img-box">
              <img src={barber.src} alt={barber.name} loading="lazy" />
              <span class="portrait-loc-tag">{barber.loc}</span>
            </div>
            <div class="portrait-meta">
              <h3 class="portrait-name font-anton">{barber.name}</h3>
              <p class="portrait-title text-gold text-sm">{barber.title}</p>
            </div>
          </div>
        ))}
      </div>

      <div class="gallery-cta">
        <a href="/barbers/" class="btn btn--solid">
          <span>Read Barber Profiles &amp; Specialisms</span> &rarr;
        </a>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  .gallery-hero {
    position: relative;
    padding-block: var(--s-9) var(--s-8);
    overflow: hidden;
    min-height: 48vh;
    display: flex;
    align-items: center;
  }
  .gallery-hero__bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    z-index: 1;
  }
  .gallery-hero__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, rgba(8, 7, 6, 0.95) 0%, rgba(14, 12, 10, 0.88) 50%, rgba(8, 7, 6, 0.72) 100%);
    z-index: 2;
  }
  .gallery-hero__content {
    position: relative;
    z-index: 3;
    max-width: var(--max);
  }
  .gallery-hero__title {
    font-size: var(--step-4);
    margin-top: var(--s-2);
    margin-bottom: var(--s-4);
  }
  .gallery-hero__lede {
    max-width: 52ch;
  }

  /* Gallery Grid */
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(22rem, 1fr));
    gap: var(--s-6);
    margin-top: var(--s-6);
  }
  .gallery-card {
    background: var(--ink-card);
    border: 1px solid var(--ink-card-border);
    border-radius: 8px;
    overflow: hidden;
    margin: 0;
    display: flex;
    flex-direction: column;
    box-shadow: var(--shadow-sm);
    transition: transform 200ms ease, border-color 200ms ease;
  }
  .gallery-card:hover {
    transform: translateY(-3px);
    border-color: var(--bronze-gold);
  }
  .gallery-card__img-wrap {
    aspect-ratio: 16 / 10;
    overflow: hidden;
    background: var(--ink-surface);
  }
  .gallery-card__img-wrap img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 300ms ease;
  }
  .gallery-card:hover .gallery-card__img-wrap img {
    transform: scale(1.04);
  }
  .gallery-card__caption {
    padding: var(--s-4);
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  .gallery-card__title {
    font-size: var(--step-0);
    color: var(--paper);
  }
  .gallery-card__desc {
    font-size: var(--step--1);
    color: rgba(250, 247, 242, 0.7);
    line-height: 1.45;
  }

  /* Barbers Grid */
  .barbers-photo-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(13rem, 1fr));
    gap: var(--s-5);
    margin-top: var(--s-6);
  }
  .barber-portrait-card {
    background: var(--ink-card);
    border: 1px solid var(--ink-card-border);
    border-radius: 8px;
    overflow: hidden;
  }
  .portrait-img-box {
    position: relative;
    aspect-ratio: 4 / 5;
    background: var(--ink-surface);
  }
  .portrait-img-box img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .portrait-loc-tag {
    position: absolute;
    bottom: var(--s-2);
    left: var(--s-2);
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    background: rgba(14, 12, 10, 0.9);
    color: var(--bronze-bright);
    border: 1px solid rgba(201, 169, 135, 0.3);
  }
  .portrait-meta {
    padding: var(--s-3);
  }
  .portrait-name {
    font-size: var(--step-1);
    line-height: 1;
    margin-bottom: 0.2rem;
  }

  .gallery-cta {
    margin-top: var(--s-7);
    text-align: center;
  }
</style>
